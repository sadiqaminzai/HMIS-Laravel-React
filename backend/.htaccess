<IfModule mod_rewrite.c>
    RewriteEngine On

    # Serve the Laravel public directory when app is deployed in a subfolder
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
