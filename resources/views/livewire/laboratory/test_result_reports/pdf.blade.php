<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Result Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .logo {
            max-width: 100px;
            margin-bottom: 10px;
        }
        h1 {
            font-size: 18px;
            margin: 0;
            padding: 0;
        }
        .info {
            margin-bottom: 15px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 5px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .footer {
            margin-top: 20px;
            font-size: 10px;
            text-align: center;
        }
        .totals {
            margin-top: 10px;
            border: 1px solid #ddd;
            padding: 5px;
            background-color: #f9f9f9;
        }
        .totals table {
            margin-bottom: 0;
        }
        .totals table, .totals th, .totals td {
            border: none;
        }
        .badge {
            display: inline-block;
            padding: 2px 5px;
            border-radius: 3px;
            font-size: 10px;
            color: white;
        }
        .bg-success {
            background-color: #28a745;
        }
        .bg-warning {
            background-color: #ffc107;
            color: #212529;
        }
        .bg-danger {
            background-color: #dc3545;
        }
    </style>
</head>
<body>
    <div class="header">
        <!-- <img src="{{ public_path('backend/assets/images/logo.png') }}" alt="Logo" class="logo"> -->
        <h1>Life HealthCare Center</h1>
        <p>Sayed Jamaluddin Township Road, Badkhak Square, Arzan Qemat, Kabul</p>
        <p>Contact: 0766 62 62 62</p>
        <p><b>Test Result Report<b></p>
    </div>
    
    <div class="info">
        <p><strong>Date:</strong> {{ date('Y-m-d') }}</p>
        <p><strong>Time:</strong> {{ date('H:i:s') }}</p>
        <p><strong>Generated By:</strong> {{ Auth::user()->name }}</p>
    </div>
    
    <table>
        <thead>
            <tr>
                <th>Receipt ID</th>
                <th>Patient Code</th>
                <th>Patient Name</th>
                <th>Doctor Name</th>
                <th>Tests</th>
                <th>Receipt Date</th>
                <th>Status</th>
                <th>Financials</th>
            </tr>
        </thead>
        <tbody>
            @forelse($service_receipts as $receipt)
                <tr>
                    <td>{{ $receipt->id ?? 'N/A' }}</td>
                    <td>{{ $receipt->patient->id ?? 'N/A' }}</td>
                    <td>{{ $receipt->patient->name ?? 'N/A' }}</td>
                    <td>
                        {{ $receipt->employee->first_name ?? '' }}
                        {{ $receipt->employee->last_name ?? '' }}
                    </td>
                    <td>
                        @php $testNames = []; @endphp
                        @foreach($receipt->service_receipt_details as $detail)
                            @php $testNames[] = $detail->service->name ?? 'N/A'; @endphp
                        @endforeach
                        {{ implode(', ', $testNames) }}
                    </td>
                    <td>{{ $receipt->receipt_date ?? 'N/A' }}</td>
                    <td>
                        @if($receipt->lab_test_status == 0)
                            <span class="badge bg-warning">Pending</span>
                        @else
                            <span class="badge bg-success">Completed</span>
                        @endif
                        <br>
                        @if($receipt->payment_status == 'paid')
                            <span class="badge bg-success">Paid</span>
                        @else
                            <span class="badge bg-danger">{{ ucfirst($receipt->payment_status ?? 'N/A') }}</span>
                        @endif
                    </td>
                    <td>
                        <small>
                            <strong>Total:</strong> {{ number_format($receipt->total_amount ?? 0, 2) }}<br>
                            <strong>Discount:</strong> {{ $receipt->discount ?? 0 }}%<br>
                            <strong>Net:</strong> {{ number_format($receipt->net_amount ?? 0, 2) }}<br>
                            <strong>Paid:</strong> {{ number_format($receipt->paid_amount ?? 0, 2) }}
                        </small>
                    </td>
                </tr>
            @empty
                <tr>
                    <td colspan="8" style="text-align: center;">No records found</td>
                </tr>
            @endforelse
        </tbody>
    </table>
    
    <!-- Financial Totals -->
    <div class="totals">
        <h3 style="margin-top: 0;">Financial Summary</h3>
        <table>
            <tr>
                <th>Total Records:</th>
                <td>{{ $totalResults }}</td>
                <th>Total Amount:</th>
                <td>{{ number_format($service_receipts->sum('total_amount'), 2) }}</td>
            </tr>
            <tr>
                <th>Total Discount Amount:</th>
                <td>{{ number_format($service_receipts->sum('discount_amount'), 2) }}</td>
                <th>Total Net Amount:</th>
                <td>{{ number_format($service_receipts->sum('net_amount'), 2) }}</td>
            </tr>
            <tr>
                <th>Total Paid Amount:</th>
                <td>{{ number_format($service_receipts->sum('paid_amount'), 2) }}</td>
                <th>Total Due Amount:</th>
                <td>{{ number_format($service_receipts->sum('due_amount'), 2) }}</td>
            </tr>
        </table>
    </div>
    
    <div class="footer">
        <p>© {{ date('Y') }} Life HealthCare Center. All rights reserved.</p>
        <p>This is a computer-generated document. No signature is required.</p>
    </div>
</body>
</html>