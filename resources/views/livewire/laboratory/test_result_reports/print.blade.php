<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Result Report - Print</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            font-size: 12px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
            border-bottom: 1px solid #ddd;
            padding-bottom: 10px;
        }
        .logo {
            max-width: 80px;
            margin-bottom: 10px;
        }
        h1 {
            font-size: 18px;
            margin: 5px 0;
        }
        h2 {
            font-size: 16px;
            margin: 5px 0;
            color: #555;
        }
        .info {
            margin-bottom: 15px;
            display: flex;
            justify-content: space-between;
        }
        .info-section {
            width: 30%;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f5f5f5;
        }
        .footer {
            margin-top: 20px;
            border-top: 1px solid #ddd;
            padding-top: 10px;
            text-align: center;
            font-size: 10px;
        }
        @media print {
            body {
                margin: 0;
                padding: 10px;
            }
            button {
                display: none;
            }
        }
        .print-button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            margin-bottom: 20px;
        }
        .print-button:hover {
            background-color: #45a049;
        }
        .badge {
            display: inline-block;
            padding: 2px 5px;
            border-radius: 3px;
            font-size: 10px;
            color: white;
        }
        .bg-success {
            background-color: #28a745;
        }
        .bg-warning {
            background-color: #ffc107;
            color: #212529;
        }
        .bg-danger {
            background-color: #dc3545;
        }
        .totals {
            margin-top: 10px;
            border: 1px solid #ddd;
            padding: 10px;
            background-color: #f9f9f9;
            margin-bottom: 20px;
        }
        .totals table {
            margin-bottom: 0;
        }
        .totals table, .totals th, .totals td {
            border: none;
        }
    </style>
</head>
<body>
    <button onclick="window.print()" class="print-button">Print Report</button>
    
    <div class="header">
        <!-- <img src="{{ public_path('backend/assets/images/logo.png') }}" alt="Logo" class="logo"> -->
        <h1>Life HealthCare Center</h1>
        <p>Sayed Jamaluddin Township Road, Badkhak Square, Arzan Qemat, Kabul</p>
        <p>Contact: 0766 62 62 62</p>
        <p><b>Test Result Report<b></p>
        <p>Generated on: {{ date('d-m-Y H:i:s') }}</p>
    </div>
    
    <div class="info">
        <div class="info-section">
            <p><strong>Report Type:</strong> Test Result Report</p>
            <p><strong>Generated By:</strong> {{ Auth::user()->name }}</p>
        </div>
        <div class="info-section">
            <!-- Filters applied -->
            @if(!empty(session('print_filters')))
                <p><strong>Filters Applied:</strong></p>
                <ul style="list-style-type: none; padding-left: 0;">
                    @if(!empty(session('print_filters')['search']))
                        <li>Search: {{ session('print_filters')['search'] }}</li>
                    @endif
                    @if(!empty(session('print_filters')['search_patient_name']))
                        <li>Patient Name: {{ session('print_filters')['search_patient_name'] }}</li>
                    @endif
                    @if(!empty(session('print_filters')['search_reporting_date']))
                        <li>Reporting Date: {{ session('print_filters')['search_reporting_date'] }}</li>
                    @endif
                    @if(!empty(session('print_filters')['searchFromDate']) && !empty(session('print_filters')['searchToDate']))
                        <li>Date Range: {{ session('print_filters')['searchFromDate'] }} to {{ session('print_filters')['searchToDate'] }}</li>
                    @endif
                </ul>
            @endif
        </div>
        <div class="info-section">
            <p><strong>Total Records:</strong> {{ $totalResults }}</p>
            <p><strong>Report ID:</strong> TR-{{ date('YmdHis') }}</p>
        </div>
    </div>
    
    <table>
        <thead>
            <tr>
                <th>Receipt ID</th>
                <th>Patient Code</th>
                <th>Patient Name</th>
                <th>Doctor Name</th>
                <th>Tests</th>
                <th>Receipt Date</th>
                <th>Status</th>
                <th>Financials</th>
            </tr>
        </thead>
        <tbody>
            @forelse($service_receipts as $receipt)
                <tr>
                    <td>{{ $receipt->id ?? 'N/A' }}</td>
                    <td>{{ $receipt->patient->id ?? 'N/A' }}</td>
                    <td>{{ $receipt->patient->name ?? 'N/A' }}</td>
                    <td>
                        {{ $receipt->employee->first_name ?? '' }}
                        {{ $receipt->employee->last_name ?? '' }}
                    </td>
                    <td>
                        @php $testNames = []; @endphp
                        @foreach($receipt->service_receipt_details as $detail)
                            @php $testNames[] = $detail->service->name ?? 'N/A'; @endphp
                        @endforeach
                        {{ implode(', ', $testNames) }}
                    </td>
                    <td>{{ $receipt->receipt_date ?? 'N/A' }}</td>
                    <td>
                        @if($receipt->lab_test_status == 0)
                            <span class="badge bg-warning">Pending</span>
                        @else
                            <span class="badge bg-success">Completed</span>
                        @endif
                        <br>
                        @if($receipt->payment_status == 'paid')
                            <span class="badge bg-success">Paid</span>
                        @else
                            <span class="badge bg-danger">{{ ucfirst($receipt->payment_status ?? 'N/A') }}</span>
                        @endif
                    </td>
                    <td>
                        <small>
                            <strong>Total:</strong> {{ number_format($receipt->total_amount ?? 0, 2) }}<br>
                            <strong>Discount:</strong> {{ $receipt->discount ?? 0 }}%<br>
                            <strong>Net:</strong> {{ number_format($receipt->net_amount ?? 0, 2) }}<br>
                            <strong>Paid:</strong> {{ number_format($receipt->paid_amount ?? 0, 2) }}
                        </small>
                    </td>
                </tr>
            @empty
                <tr>
                    <td colspan="8" style="text-align: center;">No records found</td>
                </tr>
            @endforelse
        </tbody>
    </table>
    
    <!-- Financial Totals -->
    <div class="totals">
        <h3 style="margin-top: 0;">Financial Summary</h3>
        <table>
            <tr>
                <th style="width: 20%;">Total Records:</th>
                <td style="width: 30%;">{{ $totalResults }}</td>
                <th style="width: 20%;">Total Amount:</th>
                <td style="width: 30%;">{{ number_format($service_receipts->sum('total_amount'), 2) }}</td>
            </tr>
            <tr>
                <th>Total Discount Amount:</th>
                <td>{{ number_format($service_receipts->sum('discount_amount'), 2) }}</td>
                <th>Total Net Amount:</th>
                <td>{{ number_format($service_receipts->sum('net_amount'), 2) }}</td>
            </tr>
            <tr>
                <th>Total Paid Amount:</th>
                <td>{{ number_format($service_receipts->sum('paid_amount'), 2) }}</td>
                <th>Total Due Amount:</th>
                <td>{{ number_format($service_receipts->sum('due_amount'), 2) }}</td>
            </tr>
        </table>
    </div>
    
    <div class="footer">
        <p>© {{ date('Y') }} Life HealthCare Center. All rights reserved.</p>
        <p>This is a computer-generated document. No signature is required.</p>
    </div>

    <script>
        // Auto-print when the page loads
        window.onload = function() {
            // Give a small delay to ensure everything loads properly
            setTimeout(function() {
                // window.print();
            }, 500);
        };
    </script>
</body>
</html>